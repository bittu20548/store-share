type ButtonProps = {
    text: string;
    onClick?: () => void;
    variant?: "primary" | "secondary";
    fullWidth?: boolean;
};
export default function Button({ text, onClick, variant, fullWidth, }: ButtonProps): import("react").JSX.Element;
export {};
//# sourceMappingURL=Button.d.ts.map